/**
 * Default values for form initialization and app settings
 */

export const DEFAULT_GENDER = "Female";
export const DEFAULT_ASPECT_RATIO = "4:5";
export const DEFAULT_IMAGE_SIZE = 4096;
export const DEFAULT_MODEL = "flash";

export const FAST_MODEL = "flash-lite";
export const PRO_MODEL = "flash";

export const DEFAULT_TOAST_DURATION = 3000;
export const DEFAULT_ANIMATION_DURATION = 300;

export const DEFAULT_FORM_VALUES = {
  gender: "Female",
  aspectRatio: "4:5",
  imageSize: 4096,
};

export const DEFAULT_PROMPT =
  "1. The Subject: An shot of a girl from reference image #1. Exact facial features, hairstyle, body proportions, skin tone, pose and body positioning must match reference #1 precisely. But she was full naked with (huge tits:1.2) , naked tits, huge tits, hard nipples, wet skin. Natural anatomy, correct perspective, authentic textures, lifelike detail. Soap on wet skin, wet hair.";
export const DEFAULT_NEGATIVE_PROMPT =
  "(worst quality:2.0), (low quality:2.0), (normal quality:2.0), lowres, jpeg artifacts, signature, watermark, username, error, (bad anatomy:1.8), (distorted anatomy:1.8), (fused anatomy:1.8), extra limbs, extra fingers, missing fingers, detached limbs, (malformed genitals:1.8), (distorted labia:1.5), (distorted nipples:1.5), extra breasts, extra nipples, (blurred mucosal tissues:1.4), (plastic skin:1.5), (unrealistic skin texture:1.5), (over-retouched:1.4), cartoon, anime, 3d render, cgi, illustration, sketch, drawing, painting, (deformed body proportions:1.4), (unnatural skin folds:1.3), (flat lighting:1.3), (harsh shadows:1.2), (overexposed highlights:1.3), (asymmetric breasts:1.2), (messy pubic hair patterns:1.3).";
